# RubyGems Mirror Verify

Verify RubyGems mirror's data.

## Basic

```console
$ vagrant up
$ vagrant ssh
$ cd /vagrant
/vagrant$ ./verify.sh
```

## Custom

Copy your projects's Gemfile to this directory, install dependency and run `./verify.sh`.
